import { splitFirst } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("splitFirst()", () => {
  it("should work", () => {
    expect(splitFirst).toBeTypeOf("function");
  });
});
