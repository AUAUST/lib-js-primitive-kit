import { splitNth } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("splitNth()", () => {
  it("should work", () => {
    expect(splitNth).toBeTypeOf("function");
  });
});
