import { prepend } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("prepend()", () => {
  it("should work", () => {
    expect(prepend).toBeTypeOf("function");
  });
});
