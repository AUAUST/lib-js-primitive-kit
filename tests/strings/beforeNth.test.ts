import { beforeNth } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("beforeNth()", () => {
  it("should work", () => {
    expect(beforeNth).toBeTypeOf("function");
  });
});
