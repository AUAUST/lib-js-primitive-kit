import { mapReplace } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("mapReplace()", () => {
  it("should work", () => {
    expect(mapReplace).toBeTypeOf("function");
  });
});
