import { afterNth } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("afterNth()", () => {
  it("should work", () => {
    expect(afterNth).toBeTypeOf("function");
  });
});
