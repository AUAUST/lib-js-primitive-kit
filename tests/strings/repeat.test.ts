import { repeat } from "@auaust/primitive-kit/strings";

import { describe, expect, it } from "vitest";

describe("repeat()", () => {
  it("should work", () => {
    expect(repeat).toBeTypeOf("function");
  });
});
