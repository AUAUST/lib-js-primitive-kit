import { power } from "@auaust/primitive-kit/numbers";

import { describe, expect, it } from "vitest";

describe("power()", () => {
  it("should work", () => {
    expect(power).toBeTypeOf("function");
  });
});
