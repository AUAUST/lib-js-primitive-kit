import { nor } from "@auaust/primitive-kit/booleans";

import { describe, expect, it } from "vitest";

describe("nor()", () => {
  it("should work", () => {
    expect(nor).toBeTypeOf("function");
  });
});
