import { and } from "@auaust/primitive-kit/booleans";

import { describe, expect, it } from "vitest";

describe("and()", () => {
  it("should work", () => {
    expect(and).toBeTypeOf("function");
  });
});
