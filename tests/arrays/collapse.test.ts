import { collapse } from "@auaust/primitive-kit/arrays";

import { describe, expect, it } from "vitest";

describe("collapse()", () => {
  it("should work", () => {
    expect(collapse).toBeTypeOf("function");
  });
});
