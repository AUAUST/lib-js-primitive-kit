import { intersection } from "@auaust/primitive-kit/arrays";

import { describe, expect, it } from "vitest";

describe("intersection()", () => {
  it("should work", () => {
    expect(intersection).toBeTypeOf("function");
  });
});
